
<div class="contents">

	<div class="display-map">
		<div class="map-embed">
			<div id="map-canvas">map</div>
		</div>
	</div>

	<div class="display-items">
		<div class="pattern4">
			<%= render :partial => "top/item", :collection => @items %>
		</div>
	</div>

	<div class='iine-button'>
		<%= form_for(@user_item, :url => {:controller => :top, :action => :create}, :remote => true) do |f| %>
			<!-- viewに表示されない -->
			<%= f.hidden_field :user_id, :value => 1, :class => "form_user_id" %>
			<%= f.hidden_field :item_id, :value => 1, :class => "form_item_id" %>
		  	<%= f.submit 'いいね!', :class => "button button-royal button-pill button-giant" %>
		<% end %>
	</div>

	<ul class="selected-item-list" style="list-style:none;">
		<li class="selected-item">
			<div class="item-info">
			hello
			</div>
		</li>
		<li class="selected-item">
			<div class="item-info">
				<p>item</p>
			</div>
		</li>
	</ul>

</div>


